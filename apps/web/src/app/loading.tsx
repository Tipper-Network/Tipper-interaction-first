import DefaultLoading from "@/components/ui/loading";

/**
 * Root-level loading page for the entire app
 * This is shown while the app is loading or during route transitions
 */
export default DefaultLoading;
