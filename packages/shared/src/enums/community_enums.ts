export enum CommunityMemberState__Enum {
  ANONYMOUS = "ANONYMOUS",
  MEMBER = "MEMBER",
  AUTHENTICATED = "AUTHENTICATED",
}

export enum CommunityPrivacy__Enum {
  PUBLIC = "PUBLIC",
  PRIVATE = "PRIVATE",
}

export enum EntityCommunityMembershipStatus__Enum {
  PENDING = "PENDING",
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  LEFT = "LEFT",
  BANNED = "BANNED",
  REJECTED = "REJECTED",
}
